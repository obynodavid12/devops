apiVersion: v1
kind: ConfigMap
metadata:
  name: lab-config
data:
  config.json: |
    {
      "environment" : "dev"
    }
# kubectl create configmap lab-config --from-file ./golang/configs/config.json
